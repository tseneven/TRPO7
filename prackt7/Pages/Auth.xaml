<Page x:Class="prackt7.Auth"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:prackt7"
      xmlns:materialDesign ="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      xmlns:validators="clr-namespace:prackt7.Validation"
      Title="Auth">
    <Page.Resources>
        <Style TargetType="TextBox" BasedOn="{StaticResource MaterialDesignOutlinedTextBox}">
            <Setter Property="AutomationProperties.Name" Value=""/>
        </Style>
    </Page.Resources>
    <StackPanel Style="{StaticResource formPanel}">
        <Label Width="40">Вход</Label>
        <StackPanel>
            <TextBox x:Name="Auth_Id" Style="{StaticResource MaterialDesignOutlinedTextBox}" Width="200" Height="50" materialDesign:HintAssist.Hint="ID" >
                <TextBox.Text>
                    <Binding Path="Name" UpdateSourceTrigger="PropertyChanged">
                        <Binding.ValidationRules>
                            <validators:FieldValidatior/>
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox.Text>
            </TextBox>
        </StackPanel>
        <StackPanel>
            <TextBox Style="{StaticResource MaterialDesignOutlinedTextBox}" Width="200" Height="50" x:Name="Auth_pass" materialDesign:HintAssist.Hint="Пароль">
                <TextBox.Text>
                    <Binding Path="Password" UpdateSourceTrigger="PropertyChanged">
                        <Binding.ValidationRules>
                            <validators:FieldValidatior/>
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox.Text>
            </TextBox>
        </StackPanel>
        <Button Click="Button_Click" Background="{DynamicResource MainButton}">Вход</Button>
        <Label x:Name="Reg_But" HorizontalAlignment="Center" >
            <Hyperlink NavigateUri="Register.xaml">Регистрация</Hyperlink> </Label>
    </StackPanel>
</Page>
